# 코드 품질 관련 패키지
# black 23.12.0: 23.3.0 rejects pyproject target-version 'py312' (CI Python).
# Pin must match what the changed-files lint gate runs locally and in CI.
black==23.12.0
flake8==6.0.0
isort==5.12.0
mypy==1.3.0
pre-commit==3.3.2

# 테스트 관련 패키지
pytest==7.3.1
pytest-cov==4.1.0
# pytest.ini가 timeout=300/timeout_method=thread 를 선언하지만 플러그인이 없으면
# 조용히 무시된다(감사 TH-5). 선언된 5분 행 가드가 실제로 작동하도록 설치.
pytest-timeout==2.3.1

# 타입 스텁
types-requests
types-setuptools

# 문서화 관련 패키지
sphinx==7.0.1
sphinx-rtd-theme==1.3.0
