- 2026-05-22 Codex: Added first-pass multi-frame CAD region hardening (LINE frame recovery, table filtering, safer region matching, outside-bbox cloud assignment guard, and guarded multi-frame feature mode status).
- 2026-05-22 Codex: Extended multi-frame region detection with INSERT virtual-entity frame expansion and paperspace VIEWPORT fallback tests.
- 2026-05-22 Codex: Added multi-frame validation output and review gate metrics for outside-region clouds, cross-region changes, and review-required region matches.
- 2026-05-22 Codex: Added testable CAD region-local entity compare core that normalizes matched detail regions before comparing and maps changes back to world bboxes.
- 2026-05-22 Codex: Added PDF preview page/effective-DPI hardening and opt-in DXF region-local compare result artifacts.
- 2026-05-22 Codex: Preserved PDF page-pair metadata through change-zone CSV/viewer overlays and filtered GUI overlays from the full per-pair cache.
- 2026-05-22 Codex: Added PDF lightweight page cache hits and cancellable viewer-package subprocess monitoring.
- 2026-05-22 Codex: Hardened lightweight viewport state transitions so scene/empty/PDF-error paths clear stale PDF backgrounds and rerender state.
- 2026-05-22 Codex: Aligned viewer render worker PDF transform metadata with in-process effective-DPI schema.
- 2026-05-26 Codex: Added multi-detail region compare agent roadmap with PRD, tech spec, stepwise implementation plan, and validation gates.
- 2026-05-26 Codex: Completed multi-detail R0 baseline diagnostics with region sidecar summarizer and whole-modelspace/viewer-bbox regression coverage.
- 2026-05-26 Codex: Completed multi-detail R1 region profile configuration with default YAML, detector hooks, and custom pattern tests.
- 2026-05-26 Codex: Completed multi-detail R2 frame candidate hardening with score evidence, tolerant polyline closure, INSERT block evidence, and table rejection reasons.
- 2026-05-26 Codex: Completed multi-detail R3 grid spatial clustering for large drawings with 15k-entity regression coverage and CAD policy gate validation.
- 2026-05-26 Codex: Completed multi-detail R4 title-area identity extraction with drawing number normalization, title evidence, and empty-title confidence degradation.
- 2026-05-26 Codex: Completed multi-detail R5 region match overrides with JSON serialization, manual match/unmatched handling, and one-to-one override tests.
- 2026-05-26 Codex: Completed multi-detail R6 region review UI wiring with Detail Region Matching action, whole-modelspace warnings, and manual_region_matches.json reuse on rerun.
- 2026-05-26 Codex: Completed multi-detail R7 region-local primary v2 artifact path with approved-match zones, unmatched detail zones, and legacy global artifact preservation.
- 2026-05-26 Codex: Completed multi-detail R8 region viewer manifest export with region-local crop package metadata, world/local transforms, and pipeline manifest wiring.
- 2026-05-26 Codex: Prepared multi-detail R9 pilot harness with manifest-driven run collection, acceptance metrics, report generation, and synthetic artifact tests; real pilot data is still required for acceptance.
- 2026-05-26 Codex: Completed multi-detail R10 guarded enablement wiring with pilot-summary gating, high-confidence multi-detail checks, single-detail/global fallback preservation, and feature-flag rollback docs.
- 2026-05-27 Codex: Ran cold multi-agent implementation review and hardened diagnostics, pilot acceptance gates, weak unique-region review matching, manual-match localized compare eligibility, and structural detail table filtering.
- 2026-05-27 Codex: Fixed large CAD compare failure visibility and fallback by recording failed pair diagnostics, allowing same-stem cached DXF reuse when strict cache keys change, and enabling CLI ezdxf fallback after canonical token-limit failures.
- 2026-05-27 Codex: Reduced large-DWG slowdown by reusing artifact-built change zones during preview generation and preselecting cached ezdxf for large DXF fallback inputs to avoid duplicate canonical imports.
- 2026-05-27 Codex: Hardened selected-zone/vector viewer failure paths by restoring local sources behind redacted manifests, avoiding source-DXF fallback for out-of-background crops, reusing legacy DXF caches for unsupported DWG vector render, and isolating fragile MULTILEADER SVG failures.
- 2026-05-27 Codex: Produced a multi-agent PDF-first viewer/performance roadmap covering render architecture, coordinate contracts, cache/perf telemetry, selected-zone fallback, multi-sheet accuracy, backend licensing risk, and staged acceptance gates.
- 2026-05-27 Codex: Completed PDF-first R1 coordinate-contract hardening with canonical CAD/PDF/image coordinate metadata, shared PDF bbox conversion helpers, v2/v3 manifest round-trip fields, v1→v2 PDF artifact space inference, lightweight viewport helper reuse, and CAD/PDF/tile transform regression tests.
- 2026-05-27 Codex: Completed PDF-first R2 performance telemetry baseline with internal append-only perf_events.jsonl, sharable perf_events_summary.json export, runtime sampler snapshots, folder pipeline stage events, zone-render cache/fidelity events, validation-summary linkage, raw JSONL removal for sharable outputs, and targeted regression tests.
- 2026-05-27 Codex: Completed PDF-first R3 source-signature cache baseline with shared source_signature helpers, stable source hashes for viewer tiles and zone crops, DWG->DXF shared cache filenames, zone-vector reuse of strict DwgDiffer cache entries before fallback, v3 manifest source_hash metadata without misusing file_hash, cache hit/miss reason summaries, and R1/R2/R3 regression coverage.
- 2026-05-27 Codex: Completed PDF-first R4 selected-zone fallback hardening with visible relative-overlay fallback crops, stable reason_code/fallback_reason_code propagation, Korean user-facing fallback messages, PDF PIL fallback reason tracking, viewer/perf summary reason and renderer counts, validation selected-zone evidence fields, and R1-R4 regression coverage.
- 2026-05-27 Codex: Completed PDF-first R5 CAD visual backend scaffold with disabled-by-default conversion contracts, killable subprocess worker with timeout/cancel reasons, render backend registry, visual asset provenance manifest, packaged worker dispatch/hiddenimports, CAD policy gate checks for non-approved default backends, and R1-R5 regression coverage.
- 2026-05-27 Codex: Completed PDF-first R5.5 viewer/package wiring for CAD visual provenance with folder-request option flow, disabled/deferred backend metadata in viewer manifests, no external conversion from the export hot path, v3 renderer capability propagation, and targeted viewer/pipeline regression tests.
- 2026-05-27 Codex: Completed PDF-first R6-A first-review performance hardening with automatic fast-first-review for large pair/source counts and high zone counts, fast-mode caps for rendered pairs/timeouts/overlay records, pre-materialization overlay limiting that preserves selected top issues, deferred overlay metadata, and R1-R6 regression coverage.
- 2026-05-27 Codex: Added PDF-first region performance execution roadmap covering multi-agent performance/PDF-first/QA findings, R6-B through R11 roadmap, bounded telemetry, lazy viewer selection, selected-zone stabilization, multi-detail scaling, and realset release gates.
- 2026-05-27 Codex: Completed R6-B telemetry overhead hardening slice with viewer_perf JSONL append plus legacy fallback summary, bounded viewer_perf.json pointer metadata, runtime sampler spool-scan throttling/final scan, scandir-based spool sizing, narrower GUI sampler dirs, and targeted R6-B regression coverage.
- 2026-05-27 Codex: Completed R6-B first-result-ready split with pipeline review_ready callbacks, GUI partial-result loading before package completion, run_manifest ready/deferred artifact contracts, package_complete result state metadata, and comparison-service regression coverage.
- 2026-05-27 Codex: Added multi-agent performance-degradation tech spec and roadmap covering GUI selection fan-out, PDF/render/tile hot paths, memory/RSS gates, telemetry schema, benchmark corpus, PDF-first visual asset policy, selected-zone orchestrator sequencing, and P0-P8 execution packages.
- 2026-05-27 Codex: Started P0 baseline freeze execution by adding a performance baseline inventory CLI and unit coverage to classify PDF/CAD/large/multi-detail run evidence, report missing perf/viewer/selected-zone/screenshot artifacts, and emit JSON/Markdown under .benchmarks.
- 2026-05-27 Codex: Hardened P0 baseline inventory with multi-detail diagnostics, large-CAD slow-candidate visibility, candidate metric tables, and explicit detection/render failure evidence for the current multi-detail smoke run.
- 2026-05-27 Codex: Added a P0-B multi-detail baseline runner that enforces region-mode env vars, launches the realset validator with runtime/viewer/selected-zone evidence options, writes perf/nonblank post-run evidence, reruns the P0 inventory gate, and records a reproducible run summary.
- 2026-05-27 Codex: Fixed the recurring malformed LWPOLYLINE DXF read failure by adding an in-memory tolerant ezdxf reader, wiring it through region detection, entity extraction, whole/zone render paths, scene packs, DwgDiffer, drawing descriptors, and cloud marking, with regression coverage for the existing AcDbPolyline-missing CAD sample.
- 2026-05-27 Codex: Refined the performance-degradation roadmap with a fresh multi-agent planning pass covering GUI hot-path fan-out, tile/overlay materialization, comparator/extractor memory, vector/render cache retention, telemetry overhead, PDF-first hybrid truth/visual contracts, P0 multi-detail sidecar evidence, and explicit P0-P8 agent execution gates.
- 2026-05-27 Codex: Completed the next roadmap slice by adding a strict P0-C multi-detail runner evidence contract, rejecting single-region smoke runs before inventory, wiring DWG cached-DXF sources into automatic region-local compare only after the region-local gate is enabled, and covering the new contracts with targeted P0-C/P0-C2 regression tests.
- 2026-05-27 Codex: Started P1-A mandatory instrumentation hardening by making perf_events summaries stream JSONL instead of materializing the full event list and by recording summary input bytes plus summary elapsed time so telemetry overhead can be gated explicitly.
- 2026-05-27 Codex: Completed the P1-B mandatory instrumentation gate slice by enabling RuntimeBudgetSampler by default in the realset validator, adding an explicit --no-runtime-budget escape hatch, and making customer-grade audits fail when runtime_budget or non-empty perf_events_summary evidence is missing or too slow to summarize.
- 2026-05-27 Codex: Completed P1-C detailed viewer telemetry by adding background-render, tile cache, tile-pyramid, manifest-materialise, GUI selection block, selected-zone stale/drop/fallback, and worker-spawn fields to viewer_perf JSONL summaries with targeted regression coverage.
- 2026-05-27 Codex: Completed P2-A GUI hot-path hardening by deferring large overlay JSON, lightweight PDF/raster loads, initial selected-zone crop/vector/focus work, bounding overlay cache by pair count and bytes, skipping hidden legacy preview async loads in lightweight-only mode, and adding cached/cold PDF load telemetry.
- 2026-05-27 Codex: Completed P2-B GUI hot-path acceptance harness with cached/cold PDF selection p95 gates, first-review no-tile-pyramid verification, 100-pair/100k-overlay RSS/cache bounds, full-tree rebuild telemetry, and fixes for active-zone reentry, partial category-cache completion, and page-navigation deferred loads.
- 2026-05-27 Codex: Completed P2-C chunked full-tree rebuild slice with cancellable generation state, budgeted category/tree-item chunks, unified tree-plan rendering, full-tree chunk telemetry, event-loop responsiveness benchmark gates, stale-cancel tests, and default 1000-overlay benchmark evidence.
- 2026-05-27 Codex: Completed P2-D full-tree worker offload by moving cold full_overlay JSON load and full-zone tree plan build off the GUI thread, preserving generation/cancel guards, adding worker telemetry/benchmark gates, preventing lightweight render-start sync overlay loads, and verifying with P2 core regression plus default hot-path benchmark evidence.
- 2026-05-27 Codex: Completed P2-E PDF page-navigation async refresh by deferring cold overlay JSON loads to the full-tree worker path, bumping generation on page switches, resetting viewer_pair page state on drawing selection, preventing active lightweight overlay sync fallbacks, page-filtering render-worker completions, and adding page-navigation benchmark gates.
- 2026-05-27 Codex: Completed P2-F PDF page-navigation race hardening by covering the real _step_pdf_page_pair_v2 path, adding rapid page-navigation stale-leaf and event-loop-gap benchmark gates, preserving current PDF page state when stale render-worker results finish, and verifying with P2 core regression plus default hot-path benchmark evidence.
- 2026-05-27 Codex: Completed P2-G hot-path acceptance expansion by adding opt-in 10k-class rapid page-navigation stress gates, stress RSS delta telemetry, and actual lightweight PDF cold/cache-hit load gates for QtPdf availability, QML background state, overlay-after-background, event-loop gap, and cache-state coverage while keeping the default benchmark fast for routine regression.
- 2026-05-27 Codex: Completed P2-H real PDF page-navigation stress by adding an opt-in multi-page QtPdf navigation probe with redacted-source fallback coverage, final-page background/overlay correctness gates, lightweight generation-drop verification, event-loop/RSS telemetry, configurable large-page dimensions, and A4/A1 benchmark evidence showing real PDF render block time.
- 2026-05-27 Codex: Completed P2-I-A large-PDF first-render responsiveness by adding an initial PDF render pixel budget, adaptive first-display DPI selection, effective-DPI telemetry, real-PDF navigation DPI-cap gates, and A1 benchmark evidence reducing cold render from 1197.877 ms to 572.922 ms and event-loop max gap from 1235.609 ms to 623.566 ms.
- 2026-05-27 Codex: Refined the performance-degradation planning with two additional read-only agent reviews, adding explicit slowdown buckets, measurement fields, benchmark/repro matrix, MVP/default-enablement rules, data-model pipeline lock, and next-step checklist covering PDF prewarm, navigation/RSS soak, selected-zone crop-first orchestration, multi-detail review gates, and byte-aware caches.
- 2026-05-27 Codex: Completed P2-I-B real-PDF prewarm/cache navigation gates with opt-in A1 benchmark coverage, metadata fast-path/cache-hit/no-visible-mutation assertions, QML image-status readiness checks, async lightweight PDF background loading, 5M initial render pixel budget, and passing A1 evidence (cached load 35.403 ms, background ready 158.949 ms, cached event-loop max 58.102 ms, no >500 ms gaps).
- 2026-05-27 Codex: Completed P2-J navigation soak/RSS regression harness with opt-in 100-pair navigation visits, post-warmup RSS slope/end/peak gates, event-loop hard-gap gates, fixture-generation-free timing, and passing evidence (120/120 visits, selection p95 4.417 ms, event-loop p95 16.932 ms/max 191.24 ms, RSS slope 0.008 MB/100 visits).
- 2026-05-27 Codex: Completed P3-A selected-zone crop-first orchestration by adding active request-id coordination for crop results/errors, deferring focus/vector enhancement until approved crop completion, preserving pending request ids across busy/background-render waits, preventing stale vector metadata rewrites, and covering stale/crop-first behavior with targeted GUI regression tests.
- 2026-05-27 Codex: Completed the P4-A overlay/tile streaming first-paint slice with per-pair tile manifest lookup, JSONL append/materialize in selection-time pair renders, per-tile overlay materialization caps, overlay declared/materialized/omitted telemetry, opt-in 100k first-paint benchmark gates, and roadmap clarification that visible-first tile payload/RSS work remains P4-B/P5 scope.
- 2026-05-27 Codex: Completed the P4-B visible-first tile payload slice by adding partial viewport tile generation, accumulated partial manifests, viewport-scoped overlay tile materialization, selection-time PairPreviewRenderWorker integration, complete-only package-cache reuse, and benchmark gates proving 18/128 materialized tiles with outside windows left pending.
- 2026-05-27 Codex: Completed the P4-C on-demand pan/zoom tile slice by emitting missing-window requests from sparse tile models, adding a debounced VisibleTileWindowWorker, wiring both legacy GPU and active lightweight camera paths, preserving stale-result generation guards, and extending benchmark gates to prove 18 -> 20 materialized tiles, requested-window readiness, repeat-window dedupe, and no eager full pyramid.
- 2026-05-27 Codex: Completed the first P5-A overlay paging slice by adding paged overlay store artifacts, truncating large legacy overlay JSON to a compatibility subset, wiring full-zone PDF page-pair rebuilds to prefer `overlay_pages_manifest`, preserving declared/materialized overlay telemetry, and covering paged store/package/workbench behavior with targeted regression tests.
- 2026-05-27 Codex: Completed P5-B paged-overlay GUI hardening with `OverlayPageStore` query helpers, bounded first-selection/page-pair materialization, PDF render-worker page-store awareness, progress/auto-advance/cluster-toggle hot-path fixes, exact legacy-json/page-file benchmark gates, and regression tests proving no full overlay cache retention for selected PDF page pairs.
- 2026-05-27 Codex: Completed P5-C DisplayList/cache hardening with byte-aware PDF DisplayList eviction, hit/miss/RSS/PIL-fallback telemetry through zone render and viewer perf summaries, benchmark-zone cache stats, inactive lightweight full-load guards, unknown-large overlay JSON deferral, and targeted P5-C regression coverage.
- 2026-05-27 Codex: Completed P5-D DXF index cache hardening with shared cache byte-budget env resolution, estimated-byte eviction/stat counters, per-request DXF cache telemetry through zone render/GUI/viewer summaries, and regression coverage for byte eviction plus viewer perf forwarding.
- 2026-05-27 Codex: Completed P5-E tile/overlay cache telemetry normalization with materialized tile/overlay payload byte accounting, tile byte-limit env fallback, viewer perf forwarding/summary fields, shared GUI overlay cache byte-budget env resolution, namespaced overlay eviction events, and targeted package/viewer/workbench regression coverage.
- 2026-05-27 Codex: Completed P5-F synthetic navigation/RSS soak validation with 100-pair/100-visit GUI hot-path gates, overlay cache byte plateau evidence, event-loop gap gates, RSS slope/end/tail gates, and documented remaining production-corpus plus disk tile-eviction risks.
- 2026-05-28 Codex: Completed P5-G1 disk tile-cache retention with pair-scoped tile/overlay directory eviction, stale manifest miss handling, hot-pair manifest touch for LRU ordering, JSONL/materialised manifest filtering for evicted payloads, tile retention telemetry forwarding, and targeted package/workbench/tile summary regression coverage.
- 2026-05-28 Codex: Completed P5-G2 synthetic disk tile-retention soak by adding opt-in GUI benchmark gates for retained byte cap, positive eviction bytes, hot-pair retention, evicted-pair cache misses, zero stale manifests, zero orphan payloads, prune latency, and event-loop gaps; recorded passing `.benchmarks/p5g_tile_retention_probe.json` evidence.
- 2026-05-28 Codex: Completed P5-G3 realset release-gate wiring with opt-in validator checks for runtime budget, viewer perf, selected-zone render evidence, nonblank visual output, and tile-manifest payload consistency; fixed same-root viewer-package manifest materialisation after the gate exposed an empty root manifest with retained tile payloads.
- 2026-05-28 Codex: Completed P5-G4 final audit/release wiring by adding `p5_g3_realset_release_gate`, auto-requiring it for customer-grade audits, enabling `--p5-g3-realset-gate` in release validation, rejecting contradictory selected-zone skip combinations, and updating release templates/checklists.
- 2026-05-28 Codex: Completed P5-G5 P5-G3 failure triage hints by mapping audit failure domains to likely subsystem owners, including tile manifest/package materialisation and disk tile-cache retention.
- 2026-05-28 Codex: Completed P5-G6 optional controlled tile-eviction gate by adding validator/audit/release flags and P5-G6 aliases, making eviction requirements imply the realset gate, and documenting the separate low-byte-cap evidence path for realset tile-cache eviction claims.
- 2026-05-28 Codex: Completed P5-G7 reproducible forced-eviction cap evidence by adding `--p5-g6-tile-cache-mb` to validator/release/audit flows, recording `DRAWING_COMPARE_TILE_CACHE_MB` env overrides in release steps and validation evidence, and making the audit fail on configured/env/byte-limit cap mismatches.
- 2026-05-28 Codex: Completed P5-G8 forced tile-eviction evidence preservation by adding separate proof-dir manifest/inventory handling, optional required proof gates, cap mismatch diagnostics, protection against proof runs inflating customer corpus counts, and multi-agent-reviewed regression coverage.
- 2026-05-28 Codex: Completed P5-G9 customer-evidence closeout orchestration by adding a one-command closeout runner, release package wiring, optional P5-G7 proof visibility in the final audit, source-checkout evidence-script preference for provenance verification, and regression coverage that proof outputs never enter the final audit corpus.
- 2026-05-28 Codex: Completed P5-G10 closeout pre-execution hardening by adding dry-run plan JSON, source-checkout/provenance preflight, validation-output sentinel checks, per-step tile-cache env isolation, duplicate/collision/proof-misroute rejection, and documented corpus/proof routing invariants for real customer evidence execution.
- 2026-05-28 Codex: Completed P5-G11 closeout subprocess failure reporting by writing `closeout_failure.json` on non-zero or spawn-error subprocess exits, documenting the failure artifact in the release packet, and locking failed-step/triage/plan-invariant expectations in closeout and release template tests.
- 2026-05-28 Codex: Completed P5-G12 closeout readiness artifact wiring by writing `closeout_readiness.json` for passed and failed preflight, documenting it in the release packet/checklist/roadmap, and locking ready/failed statuses, preflight diagnostics, output paths, inventory roots, and proof/corpus routing invariants in tests.
- 2026-05-28 Codex: Refined the performance-degradation roadmap with a multi-agent planning pass covering production-corpus evidence contracts, memory/RSS plateau gates, telemetry-overhead gates, PDF-first visual-asset boundaries, selected-zone fallback gates, multi-detail review gates, closeout readiness audit requirements, and a P5-G13 through P5-G19 agent execution board.
- 2026-05-28 Codex: Completed P5-G14 telemetry-overhead evidence by adding runtime sampler tick/spool-scan metrics, perf-events summary overhead ratio, viewer-perf summary source/input/elapsed fields, schema/version tests, and validator/audit regression coverage before the selected-zone benchmark expansion.
- 2026-05-28 Codex: Completed P5-G15 selected-zone benchmark evidence by adding durable JSON artifacts with status/gates/source/environment metadata, per-render RSS/blank/stale/fallback/cache measurements, DisplayList/DXF retained-byte gates, cache-hit DXF evidence preservation, and targeted benchmark/service regression coverage.
- 2026-05-28 Codex: Completed P5-G16 real-corpus replay evidence by adding a validation-summary/viewer artifact replay benchmark with customer-manifest gates, RSS slope/end/tail checks, blank/stale/fallback/cache gates, GUI hot-path wrapper delegation, and targeted real-corpus/wrapper regression coverage.
- 2026-05-28 Codex: Completed P5-G18 MVP audit integration and refined the performance-degradation roadmap with multi-agent findings on actual GUI soak, Windows handle/GDI/User resource plateau, worker lifecycle, PDF visual-asset policy, closeout evidence propagation, and P5-G20 through P5-G25 execution gates.
- 2026-05-28 Codex: Completed P5-G20 closeout readiness audit by adding `scripts/audit_closeout_readiness.py`, customer-package/runbook wiring, proof-vs-corpus and tile-cache-env invariant checks, and targeted closeout/release regression coverage.
- 2026-05-28 Codex: Completed P5-G21 evidence pipeline propagation by wiring P5-G16 replay JSON discovery/generation through inventory, prepare, closeout, readiness audit, final MVP audit, release packaging, customer runbook docs, and targeted propagation regressions.
- 2026-05-28 Codex: Completed P5-G22 actual GUI soak pipeline propagation by making `p5_g22_actual_gui_soak.json` a customer-grade audit gate, routing it through inventory/prepare/closeout/readiness/release packaging, hardening packaged CLI help via lazy GUI imports, and validating 226 targeted P5-G22/closeout tests.
- 2026-05-28 Codex: Refined the performance-degradation roadmap from multi-agent planning, expanding P5-G23 through P5-G30 into native resource sampling, PDF visual asset policy, multi-detail customer gates, selection latency, selected-zone crop-first soak, cache plateau, telemetry overhead, and composite customer visual performance release gates.
- 2026-05-28 Codex: Added a deeper multi-agent performance-degradation planning section covering slowdown taxonomy, native leak/resource evidence, PDF-first visual-asset boundaries, multi-detail correctness gates, customer-grade acceptance thresholds, disjoint agent work allocation, and the concrete P5-G23 through P5-G30 execution order.
- 2026-05-28 Codex: Completed P5-G23 native resource sampler implementation by adding shared process/fd/GDI/User/worker sampling, RuntimeBudget schema v4 native deltas, throttled native sampling, P5-G22 native/worker summary propagation through GUI soak/prepare/inventory/final audit, customer-grade missing-summary rejection, and targeted validation with 287 passing P5-G23 tests plus py_compile and diff-check.
- 2026-05-28 Codex: Started P5-G24 PDF visual asset policy by expanding VisualAssetManifest provenance/cache/nonblank/hot-path fields, adding deterministic visual asset cache keys and customer-grade policy validation, forcing CAD visual conversion workers to ignore env-only backend selection, adding hot-path conversion rejection to cad_policy_gate, and validating the foundation slice with 24 passing tests plus repository policy gate.
- 2026-05-28 Codex: Refined the PDF-first performance-degradation plan with a three-agent review, adding explicit GUI hot-path, PDF responsiveness, visual cache identity, tile/manifest plateau, selected-zone lifecycle, native/QML leak, telemetry-cost, and composite customer release work packages with concrete metrics and acceptance gates.
- 2026-05-28 Codex: Advanced P5-G24 by emitting viewer-package visual asset manifests for source PDFs, raster backgrounds, and skipped CAD visual provenance; adding rendered-background signatures to tile cache identity; adding a customer-grade `p5_g24_visual_asset_policy` audit gate; and validating with 170 targeted tests plus cad_policy_gate.
- 2026-05-28 Codex: Completed the next P5-G24 hardening slice by replacing hash-link-only nonblank placeholders with pixel-level probe artifacts over rendered bitmap targets, making final audit verify probe hashes and target hashes, propagating P5-G24 readiness through prepare/inventory, and validating 252 targeted tests plus cad_policy_gate and diff-check.
- 2026-05-28 Codex: Completed P5-G24 sidecar/cache identity hardening by discovering sidecar PDF visual assets without enabling conversion, promoting visual asset cache keys to v1.1 with geometry/transform quality, wiring pair visual identity into tile cache keys through stable content hashes, and validating 257 targeted tests plus cad_policy_gate and diff-check.
- 2026-05-28 Codex: Started P5-G26 selection-latency gating by adding an opt-in `p5_g26_selection_latency_soak` benchmark envelope, WP-A/WP-B aggregate contract gates for GUI/PDF hot paths, required gate-name metadata, and regression coverage for pass/fail contract payloads.
- 2026-05-28 Codex: Extended P5-G26 selection-latency gating with a synthetic zone-selection hot-path probe, explicit zone evidence gates for telemetry count/p95/background-work/stale events, and isolated the probe from lightweight PDF load/full-tree/vector/crop subprocess side effects.
- 2026-05-28 Codex: Propagated P5-G26 selection-latency evidence through prepare/inventory/final-audit tooling with manifest artifact refs, provenance hashes, customer-grade required audit checks, required-gate validation, and missing/failed gate regression tests.
- 2026-05-28 Codex: Started P5-G27 selected-zone crop-first gating by adding an opt-in `p5_g27_selected_zone_crop_soak` benchmark contract, synthetic crop-first lifecycle probe, vector-failure background-preservation gate, required P5-G27 gate metadata, CLI targets, and benchmark regression coverage.
- 2026-05-28 Codex: Hardened Windows CAD import startup by disabling Python's WMI-backed platform probe before comparison-package ezdxf imports, preventing unhealthy WMI from hanging benchmark/test startup while preserving the stdlib fallback path.
- 2026-05-28 Codex: Propagated P5-G27 selected-zone crop-first evidence through prepare/inventory/final-audit tooling with manifest artifact refs, provenance hashes, customer-grade required audit checks, required-gate validation, and 200 passing propagation regressions.
- 2026-05-28 Codex: Completed P5-G27 closeout/release routing by adding explicit crop-first JSON forwarding through closeout, readiness audit invariants, release manifest refs, README/closeout packet examples, alias/missing-file tests, and 251 passing propagation/closeout/release regressions.
- 2026-05-28 Codex: Completed the P5-G27 real-renderer bridge by letting crop-first benchmark payloads attach passed P5-G16 real-corpus replay evidence, making customer-grade final audit reject synthetic-only P5-G27 JSON, updating release guidance, and validating 152 benchmark/audit plus 31 release regressions.
- 2026-05-28 Codex: Completed P5-G27 bridge-bearing closeout auto-generation by adding a seed manifest step, scheduling `p5_g27_selected_zone_crop_soak.json` after P5-G16 replay and before final manifest preparation, routing it through prepare/final audit, adding readiness audit checks for bridge/output/order invariants, and validating 20 closeout/readiness regressions.
- 2026-05-28 Codex: Started P5-G28 cache plateau gating with an opt-in `p5_g28_cache_plateau_soak` benchmark contract, tile-cache lifecycle plateau gates, byte-limit eviction reason evidence, orphan/stale cleanup checks, hot-pair retention and evicted-pair miss checks, and focused benchmark regression coverage plus CLI smoke evidence.
- 2026-05-28 Codex: Advanced P5-G28 with standalone MVP exit audit validation, explicit/result-dir cache-plateau JSON discovery, required gate-name and gate-status checks, byte-limit eviction reason enforcement, and focused benchmark/audit regressions covering missing evidence and missing eviction reasons.
- 2026-05-28 Codex: Completed P5-G30 composite customer visual-performance release gate by adding customer-grade `p5_g30_customer_visual_performance_release_gate` to the MVP exit audit, requiring P5-G3/P5-G16/P5-G22/P5-G24/P5-G26/P5-G27 checks to be present and passing, updating release README/checklist/closeout packet guidance, and adding audit/release regression coverage.
- 2026-05-28 Codex: Added a Qt Quick disable/fallback startup path for live Workbench runs by honoring `DRAWING_COMPARE_DISABLE_QT_QUICK=1`, falling back to the legacy widget preview when Qt Quick is disabled or unavailable, and locking the import-failure/env-flag behavior with subprocess regression tests.
- 2026-05-28 Codex: Routed standalone P5-G28 cache-plateau evidence through prepare/inventory by adding manifest artifact refs, provenance hashes, strict provided-artifact validation, optional recommended pipeline flags, diagnostics for pass/fail cache plateau JSONs, and prepare/inventory regression coverage while keeping P5-G28 out of default customer-grade/P5-G30 requirements.
- 2026-05-28 Codex: Routed explicit P5-G28 cache-plateau evidence through closeout/readiness/release planning by forwarding planned JSONs to final prepare/final audit, requiring `--require-p5-g28-cache-plateau-soak` only inside that explicit closeout plan, adding readiness invariants and release-template guidance, fixing multi-agent review feedback for unplanned stray P5-G28 JSON routing, and validating 272 focused P5-G28 affected regressions plus py_compile, diff-check, and cad_policy_gate.
- 2026-05-28 Codex: Broadened P5-G28 from tile-cache-only plateau evidence to category-level cache plateau evidence for DisplayList, DXF index, visual asset, overlay, and spool namespaces, adding category retained-byte, eviction, orphan/stale, and tail-slope gates plus strict final-audit/prepare/inventory rejection for missing category breakdowns.
- 2026-05-28 Codex: Added optional strict P5-G28 live-counter bridging from validation summaries by accepting `--p5-g28-validation-summary`, normalizing viewer/runtime counters for DisplayList, DXF index, overlay, and spool retention, rejecting invalid supplied live counters in benchmark/audit/prepare/inventory, and validating with CLI smoke plus 314 affected regressions.
- 2026-05-28 Codex: Advanced P5-G28 live-counter evidence with repeatable validation-summary samples, minimum readable-source enforcement, optional retained-byte tail-slope gates, strict audit/prepare/inventory rejection for growing live counters, and validation with CLI smoke plus 317 affected regressions.
- 2026-05-28 Codex: Added the P5-G28 closeout lifecycle wrapper by letting closeout auto-generate `p5_g28_cache_plateau_soak.json` from repeated standard summaries or dedicated lifecycle validation manifests, keeping lifecycle output dirs out of final audit `--results-dir`, hardening readiness audit against generated-route/min-source/summary mismatches, and validating with a CLI dry-run smoke plus 329 affected regressions.
- 2026-06-01 Codex: Completed H5d hybrid viewer emit wiring for CAD pairs with sidecar PDF visual backgrounds, CAD-frame display overlay projection, transform-quality metadata, viewer manifest propagation, and targeted viewer-package/CAD-policy regression coverage.
- 2026-06-01 Codex: Completed S2 synthetic multi-sheet sheet-match metrics with fixture generation, benchmark JSON output under the `sheet_match_*` namespace, precision/recall/F1/manual-review reporting, and passing synthetic thresholds.
- 2026-06-01 Codex: Completed S1 residual QSG fallback log throttling by wiring `lightweight_viewport` to `once_per_session_logger.log_once` and locking first-INFO/subsequent-DEBUG behavior in GUI tests.
- 2026-06-01 Codex: Added repeatable direct Workbench compare and region sheet-match scoring scripts, verified the 5-sheet direct compare result with screenshots, passed a disk sidecar H5d hybrid viewer probe, and validated actual region-match metrics at precision/recall/F1 1.0 with no false matches.
- 2026-06-01 Codex: Validated the real `D:\도면 비교` folder through PDF, registered-DXF, original-DWG, and H5d sidecar-probe paths; documented results in `out/user_drawing_compare_20260601/VALIDATION_REPORT.md`; and hardened preflight to reject unsupported AC1032 DWGs before compare execution.
- 2026-06-01 Codex: Resolved the `D:\도면 비교` AC1032 folder workflow by auto-selecting matching converted DXFs from `dxf_registered/before` and `dxf_registered/after` for unsupported DWG file pairs, preserving original/effective input provenance in `run_manifest.json`, and validating the original DWG pair end-to-end with 33 changes and zero failed pairs.
- 2026-06-01 Codex: Extended the AC1032 converted-DXF fallback from explicit DWG file pairs to same-folder and sibling before/after folder selections, then validated selecting `D:\도면 비교` for both inputs end-to-end with `dxf_registered/before` and `dxf_registered/after` as effective inputs.
- 2026-06-01 Codex: Hardened converted-DXF fallback observability and selection policy by adding direct-compare summary provenance, review-project input-resolution notices, registered-over-clean priority tests, before/after count-mismatch rejection, and real `dxf_clean`/`dxf_compare` variant validation.
- 2026-06-01 Codex: Added ADR-004 to separate AC1032/all-version DWG native support from the current converted-DXF fallback workflow, including version-by-version expansion gates, license boundaries, and real `D:\도면 비교` validation evidence.
- 2026-06-01 Codex: Added a fast FolderComparePipeline regression that verifies unsupported DWG folder inputs are resolved to converted DXF directories before preflight/scan, and that fallback provenance reaches run_manifest and review_project input-resolution metadata.
- 2026-06-01 Codex: Added direct Workbench compare summary helper regressions so redacted sharable manifests still report effective fallback paths from result descriptors, and missing/unredacted manifests keep stable summary defaults.
- 2026-06-01 Codex: Accepted ADR-004 and linked it from CAD format policy, keeping AC1018+ DWG support on the user-provided converted-DXF workflow until version-specific native reader gates are approved.
- 2026-06-01 Codex: Updated PR #4 for the AC1032 fallback scope, added the release packet with GUI exposure decision, and started ADR-004 Phase 0 with a DWG corpus inventory script plus real `D:\도면 비교` inventory evidence.
- 2026-06-02 Codex: Expanded ADR-004 Phase 0 DWG corpus inventory with per-root summaries, Markdown report generation, fallback readiness/corpus-gap reporting, and a real local corpus report covering repo/test, documented, and available drawing roots.
- 2026-06-02 Codex: Prepared ADR-004 Phase 0-B baseline corpus candidates by selecting version-stratified DWGs, capturing the ready AC1032 converted-DXF compare summary, and documenting AC1018/AC1021/AC1024/AC1027 conversion requests plus Phase 0-C entry conditions.
- 2026-06-02 Codex: Reran `D:\04. 작성도면` with a full Phase 0-B scan, confirming 302 DWGs across AC1021/AC1024/AC1027/AC1032 plus corrupted samples, and promoted stronger AC1024/AC1027 revision-pair conversion requests.
- 2026-06-02 Codex: Added the ADR-004 Phase 0-B DXF conversion request packet for selected AC1024/AC1027 pairs, including existing-DXF checks, requested output layout, and a follow-up manifest for compare-summary capture.
- 2026-06-02 Codex: Documented the ADR-004 Phase 1 native DWG reader roadmap for AC1018/AC1021/AC1024/AC1027/AC1032, including clean-room gates, parser boundaries, baseline metrics, release-claim blocks, implementation checklist, and risk register without starting parser implementation.
- 2026-06-02 Codex: Added a DWG backend selection boundary with clean-room AC1015 default provenance plus fail-closed commercial SDK/user-converter placeholders, wiring backend mode metadata through import reports without enabling unapproved DWG conversion.
- 2026-06-02 Codex: Wired explicit `user_converter` DWG backend mode into `ComparePipeline` so unsupported DWG pairs can compare through existing user-provided converted DXF files with preserved input-resolution provenance, while default native behavior remains fail-closed; raised only that mode's default converted-DXF token budget for large customer DXFs.
- 2026-06-02 Codex: Generated a local ADR-004 version-stratified DWG/DXF sample pack with ODA for AC1018/AC1021/AC1024/AC1027/AC1032, verified DXF `$ACADVER` headers, captured import/compare smoke summaries, and documented large-DXF limits in `ADR004_VERSION_SAMPLE_PACK_REPORT.md`.
- 2026-06-02 Codex: Added a repeatable ADR-004 version sample-pack validation runner with manifest/DWG/DXF header checks, subprocess import/compare smoke timeouts, unit coverage, and a real v2 validation summary showing structural pass with AC1024+ large-CAD compare/import limits still unresolved.
- 2026-06-02 Codex: Added compact ADR-004 compare candidate selection, generated and validated smaller AC1024/AC1027 baselines, recaptured the existing AC1032 registered-DXF baseline as the compare oracle, and documented remaining AC1018/AC1021 compare corpus gaps.
- 2026-06-02 Codex: Added ADR-004 Phase 0-C baseline aggregation with a reusable validation script, generated the baseline metrics matrix showing AC1024/AC1027/AC1032 compare-ready and AC1018/AC1021 compare gaps, and validated the selection rules with unit coverage.
- 2026-06-02 Codex: Expanded ADR-004 AC1018/AC1021 candidate search across local CAD roots, rejected false sheet-to-sheet matches without revision evidence, documented that no real before/after compare baseline is available, and linked that gap evidence into the Phase 0-C metrics matrix.
- 2026-06-02 Codex: Propagated explicit and environment-selected `user_converter` DWG backend mode through `DwgDiffer` and the file-compare CLI so unsupported AC1024/AC1027/AC1032 DWG pairs can be compared through nearby user-provided converted DXFs while default native DWG handling remains fail-closed.
- 2026-06-02 Codex: Added an explicit `oda_converter` DWG backend mode for local/internal file compare runs, enabling the existing quarantined ODA fallback path only when that mode is selected while keeping default customer/native behavior disabled.
- 2026-06-02 Codex: Stabilized explicit `oda_converter` fallback with source-signature DXF cache, hit/miss provenance, conversion/import guardrails, CLI limit overrides, and real `D:\도면 비교` AC1032 validation showing cached reruns fail fast on converted-DXF token budget instead of hanging.
- 2026-06-02 Codex: Optimized closed-polyline normalization from quadratic rotation enumeration to linear minimal-rotation selection, cutting cached ODA AC1032 DXF import+normalize from >120s timeout to ~22.8s and completing the real `D:\도면 비교` ODA cached compare with raised DXF token budget in ~87.9s.
- 2026-06-02 Codex: Added drawing-compare completion criteria separating customer-ready fallback workflows from ADR-004 native DWG support claims, with support/gap matrices, release gates, partial-import policy, and Phase 1 native entry/exit gates.
- 2026-06-02 Codex: Added a GPT Pro Deep Research review prompt and access setup note for repository-based precision review, including private-repo access options, review scope, gate checks, and expected finding format.
- 2026-06-02 Codex: Added release readiness metrics and a dedicated drawing-compare audit gate so fallback-based customer-ready claims can be evaluated with evidence counts, thresholds, provenance, partial-import, cleanup, and forbidden wording checks.
- 2026-06-02 Codex: Documented the all-major-DWG-version support strategy, comparing commercial SDK, converted-DXF fallback, and clean-room native expansion tracks with version matrix, corpus gates, accuracy thresholds, backend policy, and release wording limits.
- 2026-06-02 Codex: Added regression coverage proving `user_converter` routes all major unsupported DWG codes through registered converted-DXF fallback when matching DXFs exist, without changing native DWG support claims.
- 2026-06-02 Codex: Extended converted-DXF fallback regression coverage to file and folder DWG selections for all major unsupported DWG codes plus unknown future `AC` signatures, preserving the native-support claim boundary.
- 2026-06-02 Codex: Added explicit customer-provided `user_converter` auto-conversion support with source-signature DXF cache, cache-hit provenance, failure provenance, and CLI option plumbing, keeping default/native and ODA policies isolated.
- 2026-06-02 Codex: Threaded explicit `user_converter` settings through preflight, folder compare, BatchCompare, and folder CLI so batch DWG pairs can reach customer-provided conversion instead of being stopped by native unsupported-version preflight.
- 2026-06-02 Codex: Extended folder scan descriptors and region-aware sidecar DWG source resolution to use the explicit `user_converter` cache, then validated a mock-free folder CLI AC1032 run with a temporary customer converter.
- 2026-06-02 Codex: Added a local real-world DWG manifest builder so customer/private DWG folders can be turned into validation-compatible sample and revision-pair evidence without copying drawings into source control.
- 2026-06-03 Codex: Added `audit_dwg_all_version_support.py` to produce a version-by-version fallback-ready/native-ready blocker matrix and confirmed current local evidence still fails the all-version support gate.
- 2026-06-03 Codex: Extended the all-version DWG audit with per-version `next_actions` so missing samples, revision pairs, converted-DXF baselines, and native backend evidence become an executable backlog.
- 2026-06-03 Codex: Tightened all-version audit Phase 0-C aggregation so compare-ready AC1024/AC1027 baselines count their before/after samples instead of reporting false sample-collection blockers.
- 2026-06-03 Codex: Added compact CAD diff summary accounting and per-version ADR-004 validation so AC1024/AC1027/AC1032 converted-DXF fallback evidence now reaches fallback-ready while native DWG support remains unclaimed.
- 2026-06-03 Codex: Added a local ADR-004 version-matrix sample-pack builder and DXF compare-source validation mode, generating AC1009/AC1012/AC1014/AC1015/AC1018/AC1021 legacy fallback evidence to 1/2 baselines per version while keeping the all-version audit failed until second independent baselines exist.
- 2026-06-03 Codex: Optimized CAD compare spatial matching for type-scoped indexes and unique large-query scans, validated the p5case lower-version matrix as the second AC1009/AC1012/AC1014/AC1015/AC1018/AC1021 converted-DXF baseline, and moved the fallback-scope all-version audit to passed while native-ready remains empty.
- 2026-06-03 Codex: Hardened local DWG evidence collection by pairing before/after folder structures, supporting multiple real-world validation inputs, and excluding generated `out/`/`build/` samples from customer evidence by default.
- 2026-06-03 Codex: Connected the all-version DWG fallback audit to the drawing-compare release readiness gate and added an explicit native DWG claim gate that remains failed until every target generation is native-ready.
- 2026-06-03 Codex: Added an explicit commercial DWG SDK adapter loading contract with fail-closed plugin discovery and file CLI license allowlisting, enabling approved licensed all-version backends without changing default/customer behavior.
- 2026-06-03 Codex: Propagated approved commercial DWG SDK license allowlists through preflight, folder CLI, descriptor scan, BatchCompare, run manifests, and region-aware sidecar source resolution while preserving fail-closed default/customer behavior.
- 2026-06-03 Codex: Extended ADR-004 sample-pack validation and all-version evidence aggregation so approved commercial/native DWG direct-compare runs can produce native baseline counts for the native audit without treating DXF-only fallback evidence as native support.
- 2026-06-03 Codex: Added `validate_dwg_native_backend.py` to orchestrate approved commercial/native DWG sample-pack validation, native evidence aggregation, and native-scope all-version audit with fail-closed adapter/license checks.
- 2026-06-03 Codex: Hardened native DWG backend validation so approved adapters must explicitly report support for every target DWG version before sample-pack validation or native claim audit can pass.
- 2026-06-03 Codex: Added a commercial DWG JSON bridge adapter so an approved external SDK wrapper can feed `DwgAdapterDrawing` JSON into the commercial_sdk path with explicit command, license, and supported-version gates.
- 2026-06-03 Codex: Added commercial DWG JSON bridge diagnostics to import reports and native backend validation artifacts, including resolved command, command SHA-256, args template, license id, timeout, and supported-version provenance.
- 2026-06-03 Codex: Added `validate_dwg_json_bridge_contract.py` as a pre-sample-pack smoke gate for commercial DWG JSON wrappers, verifying DWG import status, allowed license, supported versions, and bridge diagnostics.
- 2026-06-03 Codex: Wired DWG JSON bridge contract reports into the release readiness native gate so commercial JSON bridge native claims require a passing contract artifact alongside the native all-version audit.
- 2026-06-03 Codex: Added optional `--bridge-contract-json` output to `validate_dwg_native_backend.py` so commercial JSON bridge native validation can generate and enforce a sample-pack DWG contract artifact before claiming native readiness.
- 2026-06-03 Codex: Added bridge wrapper CLI options to `validate_dwg_native_backend.py` so approved commercial JSON bridge validation can be reproduced from one command without hidden environment setup.
- 2026-06-03 Codex: Added explicit commercial JSON bridge options to `cad_compare file/folder` so approved SDK wrapper runs can be exercised through the product CLI with scoped environment setup and license allowlisting.
- 2026-06-03 Codex: Hardened release readiness native claims so commercial JSON bridge audits require both a passing bridge contract and product `cad_compare` result/run evidence with bridge diagnostics.
- 2026-06-03 Codex: Added `run_dwg_product_bridge_evidence.py` to batch-generate product `cad_compare file` JSON bridge evidence from ADR-004 sample packs, including per-pair result JSON, timeout, provenance, cleanup, and bridge diagnostics.
- 2026-06-03 Codex: Extended `validate_dwg_native_backend.py` with `--product-evidence-json` so approved commercial JSON bridge validation can emit native audit, bridge contract, and product `cad_compare` evidence from one fail-closed command.
- 2026-06-03 Codex: Added `validate_dwg_product_release_gate.py` to orchestrate native DWG validation, bridge contract, product `cad_compare` evidence, and release readiness audit into one final all-version/native DWG product claim gate.
- 2026-06-03 Codex: Extended the final DWG product release gate with `--extra-sample-pack` aggregation so split legacy/modern ADR-004 sample packs can produce one native all-version audit and product evidence bundle.
- 2026-06-03 Codex: Added fallback/native blocker reconciliation to the final DWG product release gate so passed fallback corpus evidence suppresses redundant sample-collection actions and surfaces approved backend/native baseline work first.
- 2026-06-03 Codex: Extended the final DWG product release gate summary with native audit blocked-version triage and version-level next actions so failed all-version/native claims produce an actionable backlog.
- 2026-06-03 Codex: Fixed ADR-004 DWG compare validation to enable ODA fallback only for explicit `oda_converter`, captured all-target-version ODA fallback evidence, and confirmed the final product/native gate still fails correctly until an approved native/commercial bridge is available.
- 2026-06-03 Codex: Added a fallback-marked converted-DXF JSON bridge helper and native-evidence guard so external converter bridge smoke tests can run without being miscounted as native DWG support.
- 2026-06-03 Codex: Tightened native evidence aggregation so commercial JSON bridge outputs must positively declare native DWG provenance before they can count toward all-version native baselines.
- 2026-06-03 Codex: Tightened product-path DWG JSON bridge evidence so `cad_compare` outputs must preserve positive native provenance and converted-DXF bridge wrappers fail native product claim gates.
- 2026-06-03 Codex: Added `doctor_dwg_native_bridge.py` to preflight approved native/commercial DWG bridge command, license, supported-version coverage, and native provenance smoke checks before the final product gate.
- 2026-06-03 Codex: Added `tools/autocad_dwg_json_bridge.py` as an explicit Autodesk AcCoreConsole/LISP native DWG JSON bridge for approved local commercial validation, with native provenance metadata and fail-closed command discovery.
- 2026-06-03 Codex: Fixed multi-sample-pack DWG product gate aggregation so individual packs can contribute valid native evidence even when each pack alone cannot satisfy the native baseline minimum before aggregation.
- 2026-06-03 Codex: Added `tools/autodesk_dwg_json_bridge.py` plus a managed C# extractor for explicit local Autodesk TrueView/AutoCAD native DWG JSON evidence, with UTF-8 bridge output and positive native provenance.
- 2026-06-03 Codex: Reconciled fallback oracle evidence into the multi-pack native product gate and captured all-target-version native-ready TrueView evidence, while leaving the overall product goal failed on missing release readiness artifacts and metrics.
- 2026-06-03 Codex: Hardened the DWG release readiness audit to read UTF-8 BOM JSON inputs and follow product pair result JSON references for partial-import warning evidence without treating missing customer evidence or metrics as passed.
- 2026-06-03 Codex: Added golden DXF release baseline metrics/evidence counts, fixed style-only suppression plus structural/layout/OCS detection gaps, and reran native release readiness to reduce blockers to missing PDF/large/fallback sample evidence and unmeasured overlay/medium/large/progress/cancel metrics.
- 2026-06-03 Codex: Added supplemental PDF/negative/block-text-dimension/overlay evidence, merged product bridge native baselines into the final DWG product gate, and reran `dwg-product-release-gate-trueview2020-all.full.json` to `passed`.
- 2026-06-03 Codex: Addressed DWG review findings by unignoring release probe scripts, cleaning user converter temp dirs on failure, preserving DWG circle normals, and keeping commercial SDK DWG region sources on the canonical path.
- 2026-06-03 Codex: Added local DWG accuracy evidence normalization/validation tooling, corrected Gemini truth direction issues into local normalized evidence, and ignored `.local/` evidence artifacts.
- 2026-06-03 Codex: Extended local accuracy evidence normalization to derive cross-version `version_resave` negative controls from existing DWG version-matrix groups without copying raw drawing files.
- 2026-06-03 Codex: Added local synthetic DWG accuracy controls for non-structural noise, block transforms, and import edge cases, bringing the local internal-pilot corpus readiness target to passed.
- 2026-06-03 Codex: Added local accuracy metric evaluation for runnable DWG fixture evidence, reporting structural TP/FP/FN, precision/recall, skipped bridge-required pairs, and target blockers.
- 2026-06-03 Codex: Wired the accuracy metric evaluator to optional real DWG backend selection and made target-blocked coverage report as blocked instead of passed.
- 2026-06-04 Codex: Added a ZWCAD native DWG JSON bridge with COM and LISP-COM extraction modes, verified native provenance on a real AC1018 DWG, and smoke-tested one real accuracy pair through the evaluator.
- 2026-06-04 Codex: Added import caching and progress logging to the accuracy evaluator, stopped a slow uncached ZWCAD full run, and hardened the ZWCAD bridge to open DWGs read-only and close without save prompts.
- 2026-06-04 Codex: Re-ran a 3-pair ZWCAD accuracy smoke after no-save hardening; all three real pairs evaluated as TP with no skipped imports or lingering ZWCAD process.
- 2026-06-04 Codex: Added scoped cleanup for ZWCAD processes spawned by the DWG JSON bridge so failed automation sessions do not leave save prompts or orphaned CAD instances.
- 2026-06-04 Codex: Added adapter-level timeout cleanup for configured bridge child process images and verified the AC1014 timeout case now exits without a lingering ZWCAD process.
- 2026-06-04 Codex: Short-circuited accuracy evidence evaluation after a failed before-import to avoid spending a second CAD timeout on after-import, and verified the AC1012 timeout probe skips after import with no ZWCAD residue.
- 2026-06-04 Codex: Hardened ZWCAD timeout cleanup with Toolhelp PID enumeration, late-spawn grace polling, and TerminateProcess fallback; added path/content-based import quarantine and skip-reason counts for accuracy evidence runs.
- 2026-06-04 Codex: Ran a full5 ZWCAD accuracy checkpoint through 12/50 pairs, confirming timeout cleanup stability, 7 TP, 1 FN, and 4 import timeouts; reproduced AC1027_pair_016 FN and found the 5,000-entity extraction cap/large after-import timeout as the next accuracy bottleneck.
- 2026-06-04 Codex: Added cap-aware FN quarantine, ROI JSON extraction for ZWCAD/AutoLISP, and evaluator ROI retry/ROI-first modes; verified unit coverage and found AC1027_pair_016 now needs truth bbox calibration or a broader ROI strategy after zero-entity ROI probes.
- 2026-06-04 Codex: Added evaluator ROI margin sweep and per-margin timeout retry tracking; AC1027_pair_016 now records sequential ROI attempts, but still skips on after-import ZWCAD timeout with cleanup confirmed.
- 2026-06-04 Codex: Added a ZWCAD LISP-COM wall-time watchdog so blocked COM calls surface as controlled DWG_IMPORT_TIMEOUT results; rechecked AC1027_pair_016 ROI-first and confirmed no traceback or lingering ZWCAD process.
- 2026-06-05 Codex: Added ZWCAD LISP-COM timeout stage reporting and adapter `timeout_stage` details; AC1027_pair_016 now classifies the current ZWCAD bottleneck as `open_document` timeout rather than ROI bbox or LISP extraction.
- 2026-06-07 Codex: Continued Claude's re-origin comparison thread by restoring registered-space MODIFIED classification conservatively, keeping unsupported entity types as reported changes with metadata, and limiting ODA auto-convert to explicit `oda_converter` folder-compare runs.
- 2026-06-07 Codex: Runtime-tested the re-origin case through public CLI and Workbench automation, found the canonical engine path still reporting 66 changes, added conservative canonical registered-space matching, and rechecked the same fixture at 4 real changes.
- 2026-06-07 Codex: Launched the current Workbench code in a visible Windows window through Computer Use, confirmed the result panel reports 4 total changes, and manually selected added/modified zones without the earlier 66-change false-positive regression.
- 2026-06-07 Codex: Fixed re-origin selected-zone crop failures by preserving old/native bboxes, using side-specific render windows, restoring redacted live source paths, and verifying C-003 fast crop plus full-detail DXF upgrade with zero selected-zone fallback count.
- 2026-06-07 Codex: Selectively integrated Claude's `feat/p0-reliability-and-pvh-viewer` visual work onto latest `main`, preserving the newer re-origin fixes while adding reference-PDF overlays, shared lightweight camera framing, layer-filtered extents, and geometry-aware revision-cloud visibility.
- 2026-06-07 Codex: Diagnosed the visible Workbench run failure as unsupported AC1032 DWG input without converted-DXF fallback, added GUI runtime diagnostics for that preflight path, propagated explicit ODA backend env selection, and hardened zone-render QProcess timeout cleanup.
- 2026-06-08 Codex: Re-ran Workbench smoke and visible UI verification, confirmed no new app error log bytes, and hardened lightweight PDF rerender scheduling so proxy/test viewports without `_pdf_render_state` no-op cleanly instead of logging debug failures.
- 2026-06-08 Codex: Directly ran GUI compare checks for real PDF and DWG pairs, fixed PDF crop DPI scaling and Windows UTF-8 zone-render process IO, and verified selected-zone before/after render artifacts without the prior path/crop failures.
- 2026-06-08 Codex: Split selected-zone render timeouts so CAD full-detail source upgrades get a 30s budget while fast crops stay at 10s, and verified the real DWG full-detail upgrade now completes as `ezdxf-matplotlib-zone` instead of being cut off near 10s.
- 2026-06-08 Codex: Adjusted viewer performance fallback aggregation to clear transient fast-crop fallback counts when the same pair/zone later succeeds with full-detail source rendering.
- 2026-06-08 Codex: Re-ran visible Computer Use PDF/DWG GUI verification and made the direct Workbench smoke script close the window so zone-render QProcess cleanup runs before exit.
- 2026-06-08 Codex: Removed accidentally committed Claude scratch/probe artifacts from the PR branch before opening the integration PR.
- 2026-06-08 Codex: Fixed selected-zone before/after crop desynchronization by giving blank sides a shared world frame, restored v2 manifest pixel sizes from v1-style transforms, and rendered DXF text with Korean-capable fonts in overview/full-detail crops.
- 2026-06-08 Codex: Added sheet-frame based lightweight viewer alignment so detected 도곽 metadata maps both panes through a shared sheet-local coordinate system before falling back to the existing world-union camera frame.
- 2026-06-08 Codex: Propagated sheet-frame bbox metadata through CAD-CAD viewer package manifests, overlays, and transforms so lightweight pane alignment can use detected drawing frames instead of raw world extents.
- 2026-06-09 Claude: Added the missing sheet-frame PRODUCER — `sheet_frame_detector.py` (conservative outer-도곽 detection, reuses sheet_region_detector rectangle helpers, returns None when not confident) emitted from `dxf_renderer.render_with_transform` as `cad_frame_bbox` while the modelspace is already open (zero extra DXF reads; serialized verbatim into manifest before/after transforms, read by the existing GUI hook). Used the renderer's outlier-filtered extents so MULTILEADER extent contamination no longer zeroes coverage. 11 new tests (9 detector + 2 renderer-emit) + producer→consumer contract; 126 regression green; diff-check clean.
- 2026-06-09 Claude: Live-verified on the real `240111_P5 복합동_PSRC,HMB` pair (33 changes, registered-DXF fallback, no crash). Finding: the primary drawing is a 459k-wide multi-detail 상세도 with NO single modelspace 도곽 (largest closed rect 0.05% coverage; border/title block in paperspace), so the detector honestly returns None. Manifest extents show before/after share origin (dx=0, NOT re-origined): world-union framing already aligns the common content; content-extents per-side overlay was therefore CONTRAINDICATED (would scale the narrower after side 1.21× and break alignment). Evidence under `build/manual_runtime_debug/gui_compare_20260609/sheet_frame_visual_check/`.
- 2026-06-09 Claude: Diagnosed the live "first-zone ~13s blank" as the one-time cold cost of parsing+rendering the 60.9MB source DXF in the serial zone-render subprocess (subsequent zones in the same drawing are instant warm-hits). Verified a naive DXF pre-warm is ineffective (the first zone's full-detail upgrade already auto-warms on load; the subprocess is serial) before building it. Shipped the low-risk perceived-performance touch instead: the otherwise-silent full-detail upgrade now shows a non-disruptive badge hint ("고해상 실도면 준비 중… 첫 구역만 잠시…") while preserving the fast-crop status (no reset). 1 new test + 79 regression green.
- 2026-06-09 Claude: Ran a 16-agent root-cause workflow (with adversarial verification of 7 load-bearing claims against code) on the multi-detail viewer symptoms. Verified root cause: the viewer frames both cameras to the union of the two sides' FULL render extents (the whole ~459k sheet) at load, discarding the change-zone world bboxes already in _active_overlays_by_zone → every detail sub-pixel. (Verification corrected the synthesis's mis-attribution to the gated region-detector collapse, and confirmed the region per-region crop viewer manifest is dead output with zero src/gui consumers.) Plan: docs/work-memory/CONTENT_AWARE_FRAMING_PLAN.md (P1 content-frame → P2 cluster navigator → P3 title-block noise → P4 render sidecar).
- 2026-06-09 Claude: Shipped P1 content-aware framing. New pure `src/services/comparison/content_frame.py` frames to the PRIMARY change zone (priority_rank 1), inserted into the existing helper `workbench_visual_extensions.apply_shared_lightweight_camera_frame` between sheet-frame alignment and the full-extents fallback (helper-only; no monolith growth; safe-degrades to the union). Real-data pivot: the original union-of-all-zones ≈ 161% of the sheet on the real pair (changes span the full extent), so it frames to the primary zone instead. Verified end-to-end on the real 240111_P5 pair via a new camera-state dump in direct_workbench_compare.py: both panes frame to the C-001 area (~7% of the sheet, ≈15× zoom), aligned (shared frame), surviving the auto-select — fixing the misaligned/near-blank overview for deleted/added primary zones. 10 new tests + 136 regression green; diff-check clean.
- 2026-06-09 Claude: Shipped P2 cluster navigator. Added `content_frame.cluster_zone_bboxes` (single-linkage on zone bboxes, cut at the largest RELATIVE/ratio jump — scale-free + outlier-resistant, deliberately not a diagonal-fraction gap) and a new helper widget `src/gui/cluster_navigator.py` (per-detail-cluster jump buttons; hidden when <2; click pans both panes to that cluster via set_camera_to_world_bbox). Two thin monolith hooks (attach in the viewer-panel build; refresh in `_set_active_overlays_v2`). On the real 240111_P5 pair it clusters into 3 distinct details (far-left notes / mid-right / far-right); a live run confirmed the navigator shows 3 buttons and clicking the far-left cluster pans both panes (aligned) to the notes area. Thumbnails deferred (text buttons deliver the jump value). 6 new tests + 91 regression green; diff-check clean.
- 2026-06-09 Claude: Closed the post-MVP verification gaps ("remaining 20%"). (1) Re-origin: the registered DXFs are already origin-normalised (dxf_shifted == the P5 pair, both min_x=-61; the comparison re-origins upstream), so framing is never challenged by a shifted render; locked the moved/shifted framing logic with synthetic tests. (2) Both-content "modified" alignment: verified on a DIFFERENT real drawing — the 01.3PG1.pdf vs 02.3PG1_R1.pdf pair produces 9 MODIFIED zones; a composite of zone C-007 (title block / revision table) shows before/after perfectly aligned (shared PDF page space) with the revision-table change visible. (3) PDF content-framing: not needed — PDF pages are sheet-scale (A3 297×420 mm), so they have no 459k-wide sub-pixel problem (content-framing is a DWG-modelspace fix only). (4) Live single-shot capture: the QtQuick lightweight viewport captures unreliably in OS screenshots; the reliable medium is the rendered zone-crop PNGs + the QML camera dump (cameraCenterX/Y, unitsPerPixel) in direct_workbench_compare.py. 2 new framing tests; full content_frame suite green.
- 2026-06-09 Claude: Hardened zone severity so a PURE position-only repositioning of annotation/markup (TEXT/DIMENSION/MULTILEADER) on non-structural layers is demoted to "low" (kept visible with a reason, never dropped) instead of being flagged critical just for containing a DIMENSION (`_zone_severity` + new `_zone_is_annotation_reposition`, guarded by the change-category so a moved-AND-edited annotation is preserved). IMPORTANT correction from a cold re-evaluation: the real 240111_P5 pair has NO such noise — its "moved" zones turned out to be genuine changes (C-005/006 = PL20x150→PL50x150 plate size change; C-007 = a dimension change carrying a non-position category), all correctly preserved by the category guard. So this is a defensive severity-correctness guard, not a cleanup of this drawing; the earlier "3/7 zones are noise" read was wrong. 7 new tests + 45 regression green; diff-check clean.
- 2026-06-12 Codex: Added a Native CAD goal-loop runner with local ledger initialization, checkpointing, invariant checks, usage docs, and unit coverage so the CAD-native vertical-slice plan can proceed as a resumable verified loop.
- 2026-06-12 Codex: Added a native CAD bridge adapter contract, deterministic fixture bridge, contract validator, and unit coverage proving explicit planned-version imports feed the existing compare engine without changing the default DWG support policy.
- 2026-06-12 Codex: Implemented the all-version native CAD loop as a machine-readable version matrix plus invariant gate, with fixture-backed explicit-adapter coverage for every known AC code while keeping default enablement limited to the current detector policy.
- 2026-06-12 Codex: Added native CAD cache identity fingerprints and a row evidence packet CLI that validates one version row's bridge import, canonical schema output, compare diff, LOD0 budget, and cache identity without broadening default DWG support.
- 2026-06-12 Codex: Added `--fixture-row` execution mode for native CAD row evidence packets, generating local before/after fixture inputs and `.local` evidence artifacts for repeatable row-loop rehearsal before real samples are available.
- 2026-06-12 Codex: Added a local real-sample smoke loop that validates collected public DWG manifest integrity, target version-code coverage, source-use warnings, and bounded importer probe outcomes while keeping parse success separate from sample coverage.
- 2026-06-12 Codex: Narrowed AC1015 public-sample failures into structured reader stages (`object map` offset block and `object decode` standard payload block), exposed the stage in real-sample smoke reports, and downgraded AC1015 real import/compare matrix evidence to match the public corpus result.
- 2026-06-12 Codex: Added AC1015 object-decode diagnostics that carry failing object handle, offset, and payload prefix through importer details and real-sample smoke reports; current public samples narrow the standard payload block to first object handle `1`.
- 2026-06-13 Codex: Added a native CAD viewer evidence packet that combines `overview-lod0/v1`, bounded primitive metadata, optional import/tile evidence, and content-frame change-zone framing; fixture bridge coverage now proves canonical import -> scene pack -> viewer evidence without broadening DWG support.
- 2026-06-13 Codex: Added `scripts/native_cad_viewer_evidence_fixture.py` to write `.local/native_cad_viewer_evidence/fixture_pair_evidence.json`, a bounded `native-cad-viewer-evidence/v1` artifact for one deterministic fixture pair with LOD0, frame, import, comparison, and policy evidence.
- 2026-06-13 Codex: Added a fail-closed AC1015 real LINE object decode slice; `nextgis_dwg_samples/line_2000.dwg` now imports through the native reader and produces a real `native-cad-viewer-evidence/v1` LOD0 packet without expanding default DWG support.
- 2026-06-13 Codex: Added the next fail-closed AC1015 real CIRCLE object decode slice; `nextgis_dwg_samples/circle_2000.dwg` now imports through the native reader and produces a real `native-cad-viewer-evidence/v1` LOD0 packet without expanding default DWG support.
- 2026-06-13 Codex: Added the next fail-closed AC1015 real ARC object decode slice; `nextgis_dwg_samples/arc_2000.dwg` now imports through the native reader and produces a real `native-cad-viewer-evidence/v1` LOD0 packet without expanding default DWG support.
- 2026-06-13 Codex: Added the next fail-closed AC1015 real LWPOLYLINE object decode slice; `nextgis_dwg_samples/polyline2d_line_2000.dwg` now imports through the native reader and produces a real `native-cad-viewer-evidence/v1` LOD0 packet without expanding default DWG support.
- 2026-06-13 Codex: Clarified the DWG native all-version product plan in `docs/collab/DWG_ALL_VERSION_SUPPORT_STRATEGY.md`, separating current fallback/AC1015 scope from explicit licensed-bridge and default clean-room native milestones.
- 2026-06-13 Codex: Added `docs/collab/DWG_NATIVE_ALL_VERSION_LONG_TERM_PROTOCOL.md` using the project-loop-engineer protocol shape, with runtime loop, validation harness, metrics, ledgers, rollout gates, and a downstream Goal Command Prompt for DWG native row work.
- 2026-06-13 Codex: Routed lightweight viewer first paint through one `ViewerPrimitiveSource` contract and derived pack/zone cache versions from a single `RENDER_CONTRACT_VERSION`. `NativeScenePack` is recognized as a first-class source behind the seam but is fixture-validated only — the real overview producer (`build_scene_pack`) emits ezdxf, so native rendering does not yet fire in the real pipeline (tracked as `viewer_lod0_real_evidence_pending`).
- 2026-06-14 Claude: Corrected the prior "wired NativeScenePack into the real viewport load path" claim — the seam classifies native artifacts but no real producer emits them — and added a wiring test asserting the real `build_scene_pack` pipeline resolves as `ezdxf_scene_pack`, keeping native rendering honestly scoped as fixture-only until a real-pipeline producer lands.
- 2026-06-13 Codex: Added `docs/collab/DWG_NATIVE_REMAINING_EXECUTION_PLAN.md` to turn the native DWG roadmap into ordered W1-W7 workstreams after the `ViewerPrimitiveSource` seam, with W1 AC1015 evidence closure as the default next slice and license/default enablement behind HITL gates.
- 2026-06-14 Codex: Added `docs/collab/DWG_NATIVE_W1_AC1015_GOAL_COMMAND.md` as the next-step Goal Command Prompt for W1 AC1015 evidence closure, grounded by bounded real-sample smoke and AC1015 fixture row evidence.
- 2026-06-14 Codex: Executed W1 target 2 by preserving fail-closed AC1015 ARC decode behavior while carrying real object handle/type/payload diagnostics into importer and smoke evidence, reclassifying the object-decode blocker as visible unsupported content without broadening DWG support.
- 2026-06-14 Claude: Executed W1 target 1 by giving `DwgSectionReadError` structured `diagnostics` and attaching `object_handle`/`object_offset`/`object_map_entry_index` to the "object map entry outside file" failure, so `sample_AC1015.dwg`'s object-map offset failure now surfaces `18F2@1392451` in importer details and the real-sample smoke `object` column (previously empty) — symmetric with the object-decode path. Reclassified the matrix blocker to `public_ac1015_object_map_offset_visible_unsupported`; default DWG support unchanged. +1 targeted test (22 passed); matrix/policy/diff-check PASS; goal-loop invariants pass except the known stale-baseline I2 meta-trip.
- 2026-06-14 Claude: Built the missing native viewer-evidence producer for W1 Target 3. New `native_scene_pack_builder.build_native_scene_pack` flattens clean-room native-reader canonical LINE/CIRCLE/ARC/POLYLINE geometry into viewer `lines` display primitives (circle/arc tessellated; unsupported types counted, not silently dropped), yielding a real `NativeScenePack` and a real `native-cad-viewer-evidence/v1` LOD0 packet directly from a native import (no ezdxf/bridge). Added `scripts/native_cad_viewer_evidence_real.py` (real .local evidence: 4/4 nextgis AC1015 samples framed and within budget) and `test_native_scene_pack_builder.py` (5 tests incl. a real-sample integration test, closing the 245-248 "no committed test" gap). Matrix `viewer_lod0_real` flag and importable->viewable promotion deliberately NOT flipped: that is a HITL claim decision. 45-test batch + matrix/policy/diff-check PASS; goal-loop invariants pass except the known stale-baseline I2 meta-trip.
- 2026-06-14 Claude: Applied the evidence-backed W1 Target 3 matrix update (user-approved flag-only option): flipped AC1015 viewer_lod0_real false->true and removed the viewer_lod0_real_evidence_pending blocker, now justified by the committed native scene-pack producer + reproducible real viewer evidence (commit 5bfa1d1). Kept state: importable (NOT promoted to viewable) because the native producer is a parallel evidence path and the product GUI viewport still renders via ezdxf, so a viewable claim would over-imply native GUI rendering. AC1015 blockers now 2 (object-map + object-decode visible-unsupported). matrix validate / cad_policy_gate / matrix tests / git diff --check PASS.
- 2026-06-14 Claude: Advanced the DWG-native planning after W1 completion. Updated DWG_NATIVE_REMAINING_EXECUTION_PLAN.md (W1 marked DONE with commit/evidence pointers; new "Completed Workstreams", "Strategic Fork Surfaced by W1" [Fork A native-as-product-render / B evidence-only / C stop clean-room expansion], and "Recommended Next Decision" sections; baseline table updated for the native producer and the ezdxf-vs-native render reality) and added a "Status: COMPLETE" banner to DWG_NATIVE_W1_AC1015_GOAL_COMMAND.md. validate_protocol (both docs) / cad_policy_gate / git diff --check PASS. Goal-loop dirty-baseline refreshed separately on the clean worktree so the I2 invariant is honest again.
- 2026-06-14 Claude: Fork A foundation. Added `native_scene_pack_builder.build_native_scene_pack_ref(doc, output_dir)` (native pack -> persisted viewer `ScenePackRef`, reusing `write_native_scene_pack_artifacts`/`overview_lod0_payload`) plus a test proving the native pack renders through the real `resolve_viewer_primitive_source` seam (ok, producer=native_scene_pack, primitives + bbox) -- the native->viewport path now fires end-to-end, not just as evidence. NOT wired into the product viewport (`viewer_session._do_build`): native render is a strict SUBSET of ezdxf (LINE/CIRCLE/ARC/POLYLINE, AC1015 only), so it must be a fallback-when-ezdxf-fails, never a replacement (else it degrades real drawings). 6 builder tests + 24 native regression PASS; policy/diff-check clean.
- 2026-06-14 Claude: Wrote `docs/collab/OWN_VIEWER_AC1032_SPIKE_TECHSPEC.md` -- the lean de-risking plan for the confirmed north star (own AC1032-native DWG diff viewer; see own-cad-viewer-goal memory). Architecture: NEW own AC1032 clean-room reader (DWG->canonical) + the existing native_scene_pack_builder/ViewerPrimitiveSource renderer skeleton + the REUSED canonical diff/revision-cloud engine. Respects the existing dwg_cleanroom_contract.py AC1032 gate (S0 = record the approved ODA-public-spec clean-room contract BEFORE any byte decoding). Steps S0-S5 each ship a test + a kill/go signal; the core de-risk is whether the R2018 container + section compression are reproducible from the public spec clean-room. ODA/ezdxf pipeline stays default behind a flag. cad_policy_gate / git diff --check PASS.
- 2026-06-14 Claude: Own-viewer spike S0+S1. S0: recorded docs/collab/AC1032_CLEANROOM_PROVENANCE.md (approved public-spec reference + product-owner approval 2026-06-14 + clean-room no-third-party-reader posture); left dwg_cleanroom_contract.py and its safety tests UNCHANGED on purpose (AC1032 object decoding stays gated; only diagnostic exploration is approved). S1: new src/services/comparison/dwg_r2018_reader.py NAVIGATES (does NOT decode) the AC1032 R2018 container -- de-obfuscates the R2004 file header (documented LCG), verifies the AcFssFcAJMB magic, locates the section-page-map. Verified on TWO real AC1032 files (sample_AC1032.dwg, Building001-0_Floor2.dwg): status=navigable, magic_ok, section-page-map offset in bounds. 8 tests (synthetic round-trip + real-sample integration). Answers the spike's biggest unknown: the R2018 container IS navigable clean-room from the public spec. cad_policy_gate / contract+diagnostic regression (13 passed) / git diff --check PASS. Next: S2 (decompress AcDb:Handles / AcDb:AcDbObjects sections).
- 2026-06-14 Claude: Own-viewer spike S2 (decompression + section-page-map). Added decompress_r2004 + read_r2004_section_page_map to dwg_r2018_reader.py (still diagnostic-only, no object decoding). decompress_r2004 implements the public-spec R2004 LZ77 variant (literal-length / two-byte-offset / long-offset opcodes; back-ref source len-compOffset-1) implemented clean-room from the published ODA "Open Design Specification for .dwg files" (consulted sections 4.6-4.7; no GPL source copied). Verified on 2 real AC1032 files: the section-page-map decompresses to the exact header decompressed_size and yields strictly-increasing bounded page numbers (sample_AC1032: 56 entries, pages 1..58 with 55/56 unwritten zero-pages; Floor2: 77 entries). 5 new tests (handcrafted decompress round-trip + pure literal + underrun guard + 2 real-sample page maps). The spike's second unknown (is R2004 compression decodable clean-room?) is answered YES; both spike unknowns now retired. 13 tests pass; cad_policy_gate / git diff --check PASS. Next: section-MAP enumeration to locate AcDb:AcDbObjects, then S3 object decode.
- 2026-06-14 Claude: Own-viewer spike S2 complete (section-map navigation + decompression bugfix). Fixed a real bug in decompress_r2004 the larger section-map stream exposed: a literal-length byte with ANY high-nibble bit set is not a length (count 0) but IS the next opcode -- the section-page-map test missed it (its literal lengths were all < 0x10). Added read_r2004_section_map: accumulates page file offsets (0x100 + sizes per spec 4.4), locates the section-map system page via section_map_id, decompresses it, and enumerates the named data sections (spec 4.5). Verified on 2 real AC1032 files: locates AcDb:AcDbObjects (sample_AC1032: 41 pages, 1.19MB), AcDb:Handles, AcDb:Header, AcDb:Classes, etc. +3 tests (literal-length-0 regression + 2 real section-map enumeration). The full AC1032 container navigation (header -> section-page-map -> section-map -> object data sections) is now proven clean-room. 16 tests pass; cad_policy_gate / git diff --check PASS. Next: S3 (decode AcDb:AcDbObjects basic geometry -> canonical).
- 2026-06-14 Claude: Own-viewer spike S3a (reach the object bytes). Added read_r2004_data_section + decrypt_r2004_data_page_header to dwg_r2018_reader.py: reads every page of a named data section, decrypts the 32-byte XOR data page header (0x4164536b ^ file_offset, type 0x4163043B per spec 4.6), decompresses, and assembles the section buffer to its exact logical size (clamping per-page padding -- a single-page section AcDb:Handles exposed that). Verified on 2 real AC1032 files: AcDb:AcDbObjects assembles to 1,192,851 bytes (41 pages); AcDb:Handles to its exact size. Reuse inventory for the remaining S3: DwgBinaryReader already provides the full DWG bit-code toolkit (B/BB/BS/BL/BD/MC/MS/handle/string) and dwg_object_decoder has the object-type constants + LINE/CIRCLE/ARC/LWPOLYLINE geometry decoders. +2 tests; 18 tests pass; cad_policy_gate/diff-check PASS. Still diagnostic-only (no objects bit-decoded). Next: object enumeration (handle map -> object offset/type walk) then geometry decode -> canonical.
- 2026-06-14 Claude: Own-viewer spike S3b (object framing + object-type decode). Cracked the R2018 object stride against real data: each object is [MS object-size][MC handle-stream-bits][bit stream: MS bytes][RS CRC], so next = offset + (MS+MC field bytes) + MS + 2 -- the MC FIELD length (1-2 bytes, from the handle-stream size) is the variable per-object overhead that earlier MS-only walks missed. Added read_r2018_object_type (spec 2.12: bit pair + 1-2 bytes) + read_r2018_object_run to dwg_r2018_reader.py (reuses DwgBinaryReader). Verified on 2 real AC1032 files: a contiguous run from offset 4 decodes to real DWG object types (sample_AC1032: 111 objects -> LINE 32, POINT 21, DICTIONARY 19, LAYER 13, ENDBLK 11, INSERT 4, CIRCLE 1, ...). +2 tests; 20 pass; cad_policy_gate/diff-check PASS. Still diagnostic-only (types, not geometry). Remaining: full enumeration needs the R2018 AcDb:Handles index (offset encoding differs from AC1015, undecoded; sequential walk covers one contiguous run, stops at free-space gaps), then geometry decode -> canonical.
- 2026-06-15 Claude: PR #22 review follow-up (minor fixes, no behavior change to verified paths). (1) decompress_r2004: guard against reading a trailing opcode/literal byte after the output reaches decompressed_size, so a stream ending exactly at the boundary no longer spuriously raises an underrun; + regression test. (2) read_r2004_section_map accepts an optional pre-decoded page_map (backward-compatible) so read_r2004_data_section no longer decodes the section-page-map twice. (3) read_r2018_object_type: documented that the pair-2/3 raw-short byte order is unverified for high custom-object type ids (fixed entities use pair 0/1). (4) test docstring: CI runs the algorithm tests; the real-AC1032 integration tests skip without the local corpus. 21 tests pass; cad_policy_gate / git diff --check PASS.
- 2026-06-15 Claude: Own-viewer spike S3 part 1 (AcDb:Handles index -> full object enumeration; still diagnostic-only). Added parse_r2018_handle_map / read_r2018_handle_map / read_r2018_object_table to dwg_r2018_reader.py and refactored read_r2018_object_run onto a shared _frame_r2018_object helper (no behavior change; the S3b run test still passes). The handle map uses the public-spec R2004+ structure (big-endian u16 section size; (handle-delta MC, location-delta SIGNED MC) pairs; 2-byte CRC; size<=2 terminator) -- the SAME structure as the AC1015 object map in dwg_section_reader.py, with locations indexing the decompressed AcDb:AcDbObjects buffer. PROVEN on the primary acadsharp sample: 842 handle entries, handles strictly increasing, clean terminator, and ALL 111 contiguous-run object offsets located by the map (exact 1:1). This RESOLVES the prior note that the R2018 handle encoding 'drifts': the earlier 'drift to 132876' was a REAL object location, not a decode error -- the handle map decodes correctly. HONEST LIMITATION found + root-caused: object framing covers only ~55% of in-bounds handle entries (acadsharp 448/836; calpoly 1230/2936) because the multi-page AcDb:AcDbObjects logical assembly in read_r2004_data_section leaves ZERO GAPS -- it places each page at its section-map start_offset (0x7400-aligned slots) but decompressed page sizes differ from the slot spacing (early pages smaller -> gaps; later pages 29856 > slot 29696 -> overlap). Confirmed: the non-framing handle offsets point at all-zero buffer regions; naive contiguous packing does not fix it either (acadsharp 459 framed). So read_r2018_object_table surfaces unframed_count honestly rather than overclaiming, and the multi-page logical-offset assembly is now the precise next blocker before 100% AC1032 object framing (and before geometry decode, which needs exact object extents). The S3a data-section test only checked len(buffer)==size, not interior placement, which is why this stayed latent. +7 tests (3 synthetic handle-map decode + 4 real-sample: handle map decode/run-coverage on both, full 111/111 coverage on the primary sample, object-table framing-beyond-one-run). 28 r2018 tests pass; 255 dwg-related comparison tests pass (refactor regression-clean); cad_policy_gate / git diff --check PASS; AC1032 clean-room contract UNCHANGED (still blocked). Remaining S3: (a) correct the multi-page logical-offset assembly so every handle-mapped object frames, then (b) geometry decode (common-entity header field-by-field) -> canonical.
- 2026-06-15 Claude: Own-viewer spike S3 part 1b -- FIXED the multi-page logical-offset assembly (the S3-part-1 blocker; still diagnostic-only). Root cause (decisive, from raw section-map + page-header dumps): each page fills a max_decompressed_size SLOT (section descriptor word @0x0C = 0x7400 for AcDbObjects), but read_r2004_data_section decompressed each page to the page HEADER's size word (word[3]), which UNDER-reports the page's logical content (e.g. acadsharp page 8: word[3]=13024 but the page actually fills the full 29696 slot; decompressing to 29696 yields a superset whose first 13024 bytes match). That left zero gaps between pages and mis-located every later object. Fix: R2004Section now carries max_decompressed_size (read from the descriptor); read_r2004_data_section decompresses each page to min(max_decompressed_size, section.size - start_offset) instead of word[3]. Verified against the handle-map oracle: object framing 55%->88.2% (acadsharp 448->737 of 836) / 42%->75.9% (calpoly 1230->2228 of 2936); the fix ALSO cleaned up calpoly's handle-map garbage tail (the earlier 'drift': AcDb:Handles was under-decompressed too -- its map now decodes 6974 clean monotonic entries with a proper terminator instead of 7157 with a repeated-handle garbage tail). Residual unframed are non-framable handle entries (stale/deleted/other-section: 82/99 on acadsharp have insane MS sizes, only ~3 recoverable by relaxing the size bound), NOT assembly gaps. Blast radius: read_r2004_data_section is spike-only (no product caller). Tests updated to the authoritative metric -- handle-map object-table framing coverage (>=85% acadsharp regression guard, >=70% both) replaces the now-invalid run-subset-of-handlemap proxy (the contiguous run-walk now over-walks ~0.6% into free-space leftovers on the gap-free buffer; read_r2018_object_run documented as heuristic, object_table is authoritative). 29 r2018 tests pass; 256 dwg-related comparison tests pass; cad_policy_gate / git diff --check PASS; contract UNCHANGED (blocked). Remaining S3: geometry decode (common-entity header field-by-field) -> canonical, now with correctly-assembled object extents.
- 2026-06-15 Claude: Own-viewer spike S3 part 2 -- basic entity GEOMETRY decode (LINE/CIRCLE/ARC/POINT), validated 1:1 against ODA ground truth (still diagnostic-only; contract blocked). This answers the spike's core question -- "can AC1032 geometry be parsed clean-room?" -- with a definitively verified YES. Implemented the R2010+ Common Entity Data parse (ODA spec 20.4.1) + the per-entity formats (20.4.20/21, 18/27) in dwg_r2018_reader.py: _parse_common_entity_header consumes H(handle), EED loop, B(graphic), BB(entmode), BL(reactors), B(xdic), B(nolinks), ENC(colour), BD(ltscale), BB x3(lt/ps/mat flags), RC(shadow), B x3(visual styles R2010+), BS(invis), RC(lineweight); then _decode_line/circle/arc/point read the geometry (LINE: B z-flag + RD/DD x/y[/z]; CIRCLE/ARC: 3BD centre + BD radius [+ BT/BE + BD start/end for ARC]; POINT: 3BD). New decode_r2018_entity (fail-closed: None on unframable/unsupported) + read_r2018_entities (enumerate via handle map, decode supported geometry). Method (the clean-room oracle that nailed it): converted the sample DWG via the locally-installed ODA File Converter -> ezdxf to get exact LINE/CIRCLE/ARC/POINT coords as ground truth (validation only, not a product dep), then located x1 in the bit stream and reverse-validated each field against the public ODA spec PDF (the prior session's webfetched copy). Decisive spec findings: entities use ENC not CMC (0x8000=RGB BL in data stream, 0x4000=AcDbColor handle-in-stream so 0 data bits, 0x2000=transparency BL); the R2013+ has-DS-binary bit is absent without a data-store section; the object's OWN handle (H field) starts the data stream and matches the handle-map handle (0 mismatches over all decoded objects). Result on the primary sample: LINE 26/26, CIRCLE 4/4 (in-map), ARC 1/1 (incl. start/end angles), POINT 6/6 -- ALL exact vs ODA; 125 entities decoded file-wide; the second sample decodes 931 entities (928 LINE). +3 tests (ground-truth fixtures incl. the true-colour+Z and AcDbColor edge cases; handle-integrity; fail-closed). 32 r2018 tests pass; 259 dwg-related comparison tests pass; cad_policy_gate / git diff --check PASS; contract UNCHANGED (blocked). Geometry is pre-canonical (dicts), not yet wired to the diff/render pipeline -- S4 (render via native_scene_pack_builder) + S5 (diff + revision clouds) remain.
- 2026-06-15 Claude: Own-viewer spike S4 -- RENDER bridge: the own clean-room AC1032 reader now drives the SAME viewport seam the ezdxf path uses, with ZERO ODA/ezdxf calls (still diagnostic-only; contract blocked). Added to dwg_r2018_reader.py: r2018_entity_to_canonical (R2018Entity -> canonical-drawing/v1 entity dict: lowercased type + {x,y,z} geometry points the producer consumes; POINT emitted as type 'point' which the producer counts unsupported, not dropped) + build_r2018_canonical_document (decode entities -> canonical doc with entities + extents). The reader stays decoupled from the renderer (it emits a plain canonical dict; the caller feeds build_native_scene_pack). End-to-end on the primary sample: 125 decoded entities -> canonical doc -> build_native_scene_pack = 91 viewport line primitives (82 LINE + 6 CIRCLE + 3 ARC, circles/arcs tessellated) + 34 POINT counted unsupported (visible) -> build_native_scene_pack_ref writes overview_lod0.json -> resolve_viewer_primitive_source returns ok=True degraded=False render_mode=skeleton_preview producer=native_scene_pack with 91 primitives and the geometry-spanning world bbox. So the spike's S4 ("own renderer shows the drawing") is proven through the existing test-proven seam. +2 tests (synthetic canonical conversion incl. POINT; real-sample full chain decode->canonical->scene pack->viewport seam). 34 r2018 tests pass; 286 dwg/scene-pack/viewport comparison tests pass; cad_policy_gate / git diff --check PASS; contract UNCHANGED (blocked). Remaining: S5 (run a real AC1032 before/after pair through the existing diff + revision-cloud engine on own-reader canonical, clouds on the changes, ODA calls = 0).
- 2026-06-15 Claude: Own-viewer spike S5 -- diff + REVISION CLOUDS on own-reader canonical; the AC1032 own-stack vertical slice is now PROVEN end-to-end with ODA/ezdxf calls = 0 (still diagnostic-only; contract blocked). Verified that the canonical doc build_r2018_canonical_document emits is directly consumable by the EXISTING canonical diff engine (DrawingCompareEngine.compare reads doc["entities"] with the SAME {x,y,z} geometry shape; it only also needs a per-entity bbox -- added bbox + space:"model" to r2018_entity_to_canonical, the only change needed; no new diff/cloud code). S5 chain (all existing production engine, no new logic): build before/after from the own reader, synthesize one edit (move a LINE end + its bbox), DrawingCompareEngine.compare -> the matcher pairs 124/125 entities unchanged and isolates the 1 edit (proves own canonical diffs cleanly) -> diff.to_change_records -> ComparisonResult -> build_change_zones -> a change zone covers the edit -> revcloud_geometry_from_bbox -> a 24-vertex revision cloud whose bounds contain the moved line. So the spike GO ("own stack renders basic geometry + shows auto revision clouds, ODA calls = 0") is met -- the only ODA use in the whole program was offline ground-truth validation of the geometry decode (S3 part 2). +1 test (before/after diff -> zones -> cloud-on-change). 35 r2018 tests pass; 348 diff/zone/cloud/scene/r2018 comparison tests pass; cad_policy_gate / git diff --check PASS; contract UNCHANGED (blocked). SPIKE COMPLETE (S0-S5): the own clean-room AC1032 reader reads -> decodes geometry (golden vs ODA) -> renders through the viewport seam -> diffs -> revision-clouds, all ODA-free. Next (post-spike, HITL): scale entity coverage (MTEXT/INSERT/HATCH/DIMENSION/LWPOLYLINE), wire the native path into the product behind a flag, and weigh promotion vs the gate-inflation/north-star caveats before any support claim.
- 2026-06-15 Claude: Post-spike entity coverage -- added LWPOLYLINE (type 0x4D) decode, the highest-value structural entity (beam/plate outlines), validated 1:1 vs ODA ground truth (still diagnostic-only; contract blocked). Implemented _decode_lwpolyline_geometry per ODA spec 20.4.85: BS flag, conditional const-width/elevation/thickness/extrusion (flag bits 4/8/2/1), BL numpoints, conditional BL numbulges/vertexIds/numwidths (flag bits 16/1024/32), then the vertices (first 2RD, the rest 2DD with the previous coordinate as default) + per-vertex bulges (BD). The closed flag is bit 0x200 (verified: closed polylines carry 0x200/0x210, open carry 0x000/0x020/0x030). Wired LWPOLYLINE -> canonical "polyline" (vertices {point:{x,y,z}, bulge}; the existing scene-pack producer already flattens "polyline" to viewport line segments). Validation vs ODA ground truth (the converted sample has 21 LWPOLYLINEs incl. arc-bulge + closed cases): 13/13 in-map polylines decode exactly (vertices + bulges + closed). The file now decodes 15 LWPOLYLINE (+82 LINE/6 CIRCLE/3 ARC/34 POINT); the scene pack grows 91 -> 106 viewport primitives. +1 geometry fixture (open no-bulge + closed-with-bulge) + LWPOLYLINE canonical-conversion assertions; updated the S4 render test's expected primitive count to include polylines. 35 r2018 tests pass; 348 dwg/scene/diff/cloud comparison tests pass; cad_policy_gate / git diff --check PASS; contract UNCHANGED (blocked). Next entity candidates by structural frequency: TEXT/MTEXT (needs string-stream decode for the text value), INSERT (block refs), DIMENSION, HATCH.
