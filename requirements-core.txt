# ODA-free CAD comparison core
#
# Intended for headless CLI/regression use and product embeddable review.

ezdxf[draw]>=1.1.0,<2.0.0
jsonschema>=4.21.0,<5.0.0
numpy>=1.24.0,<3.0.0
openpyxl>=3.1.0,<4.0.0
opencv-python-headless>=4.8.0,<5.0.0
packaging>=21.0
pandas>=2.0.0,<3.0.0
Pillow>=10.0.0,<12.0.0
psutil>=5.9.0
pyyaml>=6.0.1
rich>=12.5.0
rtree>=1.0.0,<2.0.0
# Required runtime modules (release_environment_check.py): without them the
# optimal matcher silently degrades to greedy (dxf_comparator.py) and SSIM is
# disabled (drawing_differ.py). Both are permissively licensed (BSD) — no
# copyleft concern, unlike the deliberately-excluded proprietary CAD deps.
scikit-image>=0.21.0,<1.0.0
scipy>=1.10.0,<2.0.0
