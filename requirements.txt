# 🚀 TEKLA_MCP 프로젝트 필수 의존성 (정리됨)
# 중복 제거 및 최적화 완료 - 2025-07-06

# ===== 웹 프레임워크 =====
Flask==2.3.3
Werkzeug==2.3.7
Flask-SocketIO==5.3.6
fastapi>=0.104.0
uvicorn>=0.23.0

# ===== HTTP 및 통신 =====
httpx==0.28.0
requests==2.31.0
websockets==11.0.3
python-socketio==5.10.0
python-engineio==4.8.0
websocket-client==1.7.0
aiohappyeyeballs==2.3.0

# ===== 데이터 검증 및 스키마 =====
pydantic>=2.7.0
pydantic-settings>=2.0.0
jsonschema==4.21.0

# ===== 데이터 처리 =====
pandas>=2.0.0
numpy>=1.24.0
pyarrow==15.0.0
polars>=0.20.0
scipy>=1.10.0             # Phase 3: 고급 수치 계산

# ===== 시각화 =====
matplotlib>=3.4.0
plotly>=5.15.0            # Phase 3: 대화형 차트

# ===== 파일 처리 =====
ezdxf>=1.1.0              # DXF 파일 처리
openpyxl>=3.0.9           # Excel 파일 처리
chardet>=4.0.0            # 인코딩 감지
python-dotenv>=1.0.0      # 환경 변수 처리
pyyaml>=6.0.1             # YAML 처리
Pillow>=10.0.0            # 이미지 처리 (DXF 렌더링, 도면 비교)

# ===== 도면 비교 (Drawing Comparison) =====
opencv-python>=4.8.0      # 이미지 처리 및 도면 정렬 (SSIM, Feature Matching)
PyMuPDF>=1.26.7,<2        # PDF 렌더링 및 텍스트 추출 (도면 비교용)
                          # 1.26.5/1.26.6 = CVE-2026-3029 (path traversal,
                          # arbitrary file write) — 1.26.7+ 강제 핀.
                          # 설치 참고: pip install PyMuPDF
                          # DWG 지원: ODA File Converter 별도 설치 필요
                          # https://www.opendesign.com/guestfiles/oda_file_converter

# ===== 공간 인덱싱 =====
rtree>=1.0.0              # R-tree 공간 인덱스 (DXF 비교 성능 최적화)
                          # 미설치 시 선형 검색 fallback 제공
                          # Windows: libspatialindex 별도 설치 필요
                          # 설치 가이드: docs/SPATIAL_INDEX_IMPLEMENTATION.md

# ===== 유틸리티 =====
tqdm>=4.65.0              # 진행률 표시
colorama>=0.4.6           # 컬러 출력
arrow==1.3.0              # 날짜/시간 처리
python-dateutil==2.9.0    # 날짜 유틸리티
psutil>=5.9.0             # 시스템 모니터링
memory-profiler>=0.60.0   # Phase 3: 메모리 프로파일링
cachetools>=5.3.0         # Phase 3: 캐싱 유틸리티

# ===== 로깅 및 출력 =====
loguru>=0.6.0             # 고급 로깅
colorlog>=6.7.0           # 컬러 로깅
rich>=12.5.0              # 리치 텍스트 출력

# ===== 템플릿 엔진 =====
jinja2>=3.1.2

# ===== 타입 힌트 =====
typing-extensions>=4.0.0

# ===== LLM 관련 =====
openai==1.11.0
anthropic==0.15.0
langchain==0.1.0

# ===== Tekla API (Windows only) =====
pythonnet>=3.0.0; sys_platform == 'win32'
pywin32>=305; sys_platform == 'win32'
clr-loader==0.2.6; sys_platform == 'win32'

# ===== 병렬 처리 =====


# ===== 테스트 프레임워크 =====
pytest>=7.3.1
pytest-xdist==3.5.0
pytest-cov>=4.1.0
pytest-benchmark>=4.0.0   # Phase 3: 성능 벤치마크
pytest-mock>=3.11.0       # Phase 3: 모킹 지원

# ===== 개발 도구 =====
black>=21.5b2             # 코드 포매터
flake8>=3.9.0             # 린터
mypy>=0.812               # 타입 체커
pylint>=2.8.0             # 코드 분석

# ===== GUI 및 3D =====
PySide6>=6.5.0
pyqtgraph>=0.13.3
PyOpenGL>=3.1.6

# ===== AI/ML (선택적) =====
# scikit-learn>=1.3.0     # 머신러닝
# tensorflow>=2.13.0      # 딥러닝
# joblib>=1.3.0           # 모델 저장/로드
